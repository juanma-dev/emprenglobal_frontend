import React, { useState, useEffect } from "react";
import { BrowserRouter } from "react-router-dom";

const Emprenglobal = (props) => {
  return (
    <BrowserRouter>
      <div>TEST</div>
    </BrowserRouter>
  );
};

export { Emprenglobal as default };
